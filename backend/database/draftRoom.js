class DraftRoom {
    /**
       * Constructor for draftRoom class
       * @param {*} db Pass DB object into class
       */
    constructor(db) {
      this.db = db;
    }


}

module.exports = DraftRoom;