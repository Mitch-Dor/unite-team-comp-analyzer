{
  "name": "unite-team-comp-analyzer",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "concurrently \"cd frontend && npm i --force && npm run build --force\" \"cd backend && npm i\"",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.5.1",
    "bad-words": "^4.0.0",
    "concurrently": "^9.1.2",
    "pg": "^8.16.0"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
