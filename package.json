{
  "name": "unite-team-comp-analyzer",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "export NODE_OPTIONS=--max_old_space_size=2048 && cd frontend && npm install && npm run build && cd ../backend && npm install",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "bad-words": "^4.0.0",
    "pg": "^8.16.0"
  },
  "engines": {
    "node": "18.x"
  }
}
